import React from "react";

const Search = props => {
  return (
    <div>
      <input type="search" id="searchBar" placeholder="Type in a city name" />
      <button>FIND WEATRHER</button>
    </div>
  );
};

export default Search;
